# not null
# unique
# not noll unique  == primary  、
# 第一个被设置为not noll unique 会自动转换为 primary
# default
# 联合 唯一 unique(ip,port)
# 联合 主键 primary key(ip,port)
# 表之间的关系
    # 多对多 出现两外一张表 ，表中存放 别的表的关系 ，多个外键
